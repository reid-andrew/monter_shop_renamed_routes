<h1 align = "center">Merchants</h1>
<p align="center"><%= link_to "New Merchant", "/merchants/new" %></p>
<section class = "grid-container">
  <% @merchants.each do |merchant|%>
    <section id = <%= "merchant_#{merchant.id}"%>>
      <% if current_admin? %>
        <h2><%=link_to merchant.name, "/admin/merchants/#{merchant.id}"%></h2>
        <% if merchant.active %>
          <p><%= button_to "Disable", "/admin/merchants/#{merchant.id}", params: {type: :disable}, method: :patch %></p>
        <% else %>
          <p><%= button_to "Enable", "/admin/merchants/#{merchant.id}", params: {type: :enable}, method: :patch %></p>
        <% end %>
      <% else %>
        <h2><%=link_to merchant.name, "/merchants/#{merchant.id}"%></h2>
      <% end %>
      <p><%= merchant.city %>, <%= merchant.state %></p>
    </section>
  <% end %>
</section>
